class Config:
    def __init__(self, run_name, stitchtype, overlay, naming_template, filepath, image_path, image_path_2, max_delay_time):
        self.run_name = run_name
        self.stitchtype = stitchtype
        self.overlay = overlay
        self.naming_template = naming_template
        self.filepath = filepath
        self.IMAGE_PATH = image_path
        self.IMAGE_PATH_2 = image_path_2
        self.MAX_DELAY_TIME = max_delay_time